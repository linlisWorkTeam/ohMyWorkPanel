# LinlisWorkPanel ? ????

> ???? AI Agent ?????????????????????????

## 1. OCR ??????

### ????

| ? | ?? | ?? |
|---|---|---|
| Rust ?? | `src-tauri/src/ocr.rs` | Tesseract CLI ?????? exe + ???? |
| Tauri ?? | `src-tauri/src/commands.rs` | ?? `ocr_image` ?? |
| ?? API | `src/api.ts` | `api.ocrImage(imagePath)` ?? |
| UI ?? | `src/App.tsx` | ?? ????????? ? OCR ? ????? |

### ??

- Tesseract-OCR 5.4.0+???? `C:\Program Files\Tesseract-OCR\tesseract.exe` ? PATH ??
- ????`chi_sim`??????+ `eng`????

### ????????

1. ? `src-tauri/src/` ??? `.rs` ???????????
2. ? `src-tauri/src/lib.rs` ??? `mod xxx;`
3. ? `src-tauri/src/commands.rs` ??? `#[tauri::command] pub fn xxx(...) -> AppResult<T>`
4. ? `src-tauri/src/lib.rs` ? `invoke_handler` ????? `.invoke_handler(tauri::generate_handler![..., commands::xxx])`
5. ? `src/api.ts` ??? `xxx: (args) => invoke<ReturnType>("xxx", { args })`
6. ? `src/App.tsx` ??? `api.xxx()`

---

## 2. ???????PresetRoles?

### ??

?????????????????????? Agent ???

### ???

```
db.rs: init_db() ? ???? 3 ??? JSON ? app_settings
db.rs: get_preset_roles() ? ? app_settings ?? JSON ????
commands.rs: get_preset_roles_command ? ?????
commands.rs: create_group ? ?? preset_roles ?? ? ???????? ? INSERT members + agent_profiles
```

### ???????

?? `src-tauri/src/db.rs` ? `init_db()` ? `default_roles` JSON ???

```rust
let default_roles = serde_json::json!([
    {"name":"???","adapter":"codex","roleDescription":"????","avatarColor":"#?????"},
    ...
]);
```

???????
- `name` ? ?????
- `adapter` ? Agent ????`mock`/`codex`/`claude-code`/`opencode`/`cursor`?
- `roleDescription` ? ?????????????
- `avatarColor` ? ?????

???? `api.getPresetRoles()` ???????????? `api.createGroup({..., presetRoles: ["????", ...] })`?

---

## 3. Windows ???????

### ????

Windows ? CLI ?????????
- ?? `.exe`?? PATH ??
- npm ?????`%APPDATA%\npm\<name>.cmd`?
- `.cmd` / `.bat` ?????
- `.ps1` PowerShell ??

???? `.cmd`/`.bat`/`.ps1` ??? PE ?????

### ????

`src-tauri/src/adapters/mod.rs` ?????

| ?? | ?? |
|---|---|
| `find_executable_path()` | PATH ??????????????`.exe`/`.cmd`/`.bat`/`.ps1`??????? |
| `find_in_npm()` | `%APPDATA%\npm` ??????? |
| `prepare_command()` | ?????????????`.cmd`/`.bat` ? `cmd /c`?`.ps1` ? `powershell -File`?`.exe` ? ???? |

### ????????

1. ?? `src-tauri/src/adapters/<name>.rs`
2. ?? `pub fn build_args(prompt: &str) -> Vec<String>`
3. ? `mod.rs` ? `AdapterKind` ????????
4. ? `AdapterKind::as_str()` ? `AdapterKind::parse()` ?????
5. ? `candidate_executables` ??? CLI ???
6. ? `types.rs` ? adapter union ???????
7. ? `App.tsx` ? `<select>` ?????

---

## 4. ?????

??? SQLite `app_settings` ??????

| ? | ??? | ?? |
|---|---|---|
| `max_concurrent_runs` | 3 | ????????? |
| `run_timeout_seconds` | 900 | ????????? |
| `context_message_limit` | 40 | ??? Agent ??????? |
| `max_delegation_depth` | 2 | ??? Agent ?????? |

???????????????

---

## 5. ??? schema ????

1. ? `init_db()` ? `execute_batch` ??? `CREATE TABLE IF NOT EXISTS ...`
2. ? `models.rs` ?????? Rust ????derive `Serialize` + `Deserialize`?
3. ? `db.rs` ????????????????`xxx_from_row`?
4. ? `db.rs` ?????/????
5. ? `commands.rs` ??? `#[tauri::command]`
6. ??? `types.ts` ??? TS ??
7. ??? `api.ts` ??? API ??
