# 墓志铭 — 2026-07-19 会话

本轮会话完成了四项工作：仓库正规化、后端模块拆分、补齐 OpenCode/Cursor 适配器、混合验证。

## 仓库正规化

- 删除空 `.git`，重新 init，建立基线提交
- 新增 `.gitignore`：排除 `node_modules/`、`dist/`、`src-tauri/target/`、`src-tauri/gen/`、`.pnpm-store/`、`*.sqlite3*`
- 新增 `README.md`：技术栈、快速开始、适配器表、验证说明
- 补充最小 `src-tauri/icons/icon.ico` + `icon.png`（32x32 占位）

## 后端模块拆分

将原 `src-tauri/src/lib.rs`（~470 行单体）拆为：

| 文件 | 行数 | 职责 |
|---|---|---|
| `lib.rs` | 57 | 启动入口、模块声明、AppState |
| `models.rs` | ~140 | serde 类型定义 |
| `db.rs` | ~360 | SQLite 初始化、CRUD、映射函数、单测 |
| `commands.rs` | ~380 | 全部 `#[tauri::command]` IPC |
| `scheduler.rs` | ~310 | 调度/执行/委派/完成 |
| `adapters/mod.rs` | ~230 | AdapterKind 枚举、解析回退、运行循环、单测 |
| `adapters/parse.rs` | ~50 | JSON 流文本抽取 + 单测 |
| `adapters/mock.rs` | ~5 | 模拟流式 chunk |
| `adapters/codex.rs` | ~7 | `codex exec --json --skip-git-repo-check` |
| `adapters/claude.rs` | ~9 | `claude -p --output-format stream-json --verbose` |
| `adapters/opencode.rs` | ~5 | `opencode run --format json` |
| `adapters/cursor.rs` | ~9 | `agent -p --output-format stream-json`；回退 `cursor-agent` |

## 关键决策

| 项 | 选择 |
|---|---|
| 适配器分发方式 | `AdapterKind` 枚举 + match（不引入 `async_trait`） |
| Cursor 可执行文件 | 优先 `agent`，回退 `cursor-agent` |
| `schedule_group` / `append_delta` | 改为同步函数，避免 `MutexGuard` 跨 await 导致 `!Send` |
| 拆分粒度 | 中拆：函数按职责分文件，共享 trait/元口放 `adapters/mod.rs` |
| 验证策略 | 混合 C：单测必过 + smoke 尽力，非安装记为 SKIPPED |

## 编译与单测

- `cargo test`：8 通过
  - `adapters::parse`：JSON 文本抽取 + 裸文本回退
  - `adapters::tests`：参数快照、Cursor 路径回退、适配器解析
  - `db::tests`：数据库中断标记
- 前端 `vitest`：1 通过（`mentions.test.ts`）

## 残留 warnings

- `adapters::AdapterKind::default_executable` 被 `resolve_executable` 间接调用但编译期内联后标记为 `dead_code`：无功能影响，可在后续单测引用消除

## 提交历史

```
eb3853e chore: ignore Tauri generated schema output
480c117 test: add adapter unit coverage and smoke script
38e99a7 refactor: split lib.rs into modules with adapter runners
aeee268 chore: init git, gitignore, and README
```

## 当前状态

- 分支：`master`，无远程
- 前端可启动：`pnpm tauri dev`
- 后端测试全部通过
- OpenCode / Cursor 前端标签已从「预留」移除
- CSV / JSON 接口、SQLite schema、前端组件 **接口无破坏性变更**
