---
date: 2026-09-02
topic: v2.2.0-self-marketing-acceptance
status: ready-to-merge
---

# v2.2.0 Self-Marketing 联合验收

## Outcome

- 在隔离数据目录和端口 `:18092` 使用本机已登录的原生 Codex CLI 完成真实 Planner → Writer → 人类修改 → Writer revision → 批准 → Markdown 导出。
- campaign `84666af0-1346-4c1e-9c23-e0040bc28c51` 基于 `origin/main..877fd389` 收集 22 条证据，生成 5 个渠道，revision=1，最终 `findings=[]`，导出 5,045 字符 Markdown。
- 浏览器确认群聊卡片展示 `已批准`、`r1`、Content Brief、五渠道标签和导出入口；桌面视口无控制台错误。
- ECS `:8081` 灰度构建、门禁、健康检查和前端资源通过；生产 `:8080` 未修改。
- workpanelConnecter 的 `codex-windows11` 远程 Agent 在同一 canary 群成功注册、领取和回传独立审查任务，证明 Connector membership / routing / poll / ack / result 链路未回归；其 PR #7 已合并为 `c6c0e96d4d028add3eeb996f471d2a2c22336bbf`。

## Acceptance fixes

- Planner snapshot 改用受限紧凑投影，避免 Windows CLI argv 超长；完整 snapshot 仍保存在 SQLite。
- 新增 `OHMYWORKPANEL_CODEX_NATIVE_AUTH=1`，允许显式复用 Codex CLI 登录 provider；Windows 长 prompt 建议成员配置指向原生 `codex.exe`。
- 明确 `publish` 仅表示值得生成待审草稿，不等于已正式发布。
- Reviewer 识别否定语境，允许“不保证”“不是零风险”等免责声明，同时继续阻止真正的绝对承诺。

## Evidence boundary

- ECS canary 本机 Agent 凭据无效，因此远端 Writer 没有被声称为成功；真实内容质量验收在隔离的 Windows 服务和已登录 Codex CLI 完成。
- Connector Agent 的联合任务用于验证跨机在线状态与调度，不承担 Content Brief 生成；它独立读取 campaign 证据并正确指出 ECS credential failure layer。
- 自动发布、OAuth、平台数据反馈和生产晋升均未执行，也不属于本 MVP。

## Verification commands

```bash
pnpm run check:colors
pnpm run check:ai
pnpm run test:gate
pnpm run submit:ai
```

Connector 仓同时执行 `npm test`、`test:relay-unit`、`test:relay`、`test:e2e-resume` 和 `gate:codex-runner-canary`。

最新 Connector 实证为 `DEVICE_CODEX_ECS_CANARY_OK_9d2bce43|workpanel-connecter|0.2.3`（task `codex_canary_f49cf352-c6d7-4354-82cb-83eaba432bca`）与 `SELF_MARKETING_CONNECTER_REVIEW_822401ae`（task `self_marketing_review_29195134-c703-495e-8a6c-22352e4f9e42`）；完整发布门禁返回 `RELEASE_LOCAL_GATE_OK gates=51`。灰度公告 message=`aae0c298-70a4-42c0-8a83-8c10edbf471c`，验收后群 workspace 已恢复为 `/AI/ohMyWorkPanel`。

## Follow-up

- PR 合并后保留灰度验证证据；未经 root 15 分钟批准令牌，不得晋升生产。
- 原生 Codex 登录模式保持 opt-in；默认 DeepSeek Responses 代理行为不变。
