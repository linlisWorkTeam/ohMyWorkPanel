---
date: 2026-08-23
topic: v2.1.1-release-verify
status: verified
---

# v2.1.1 发布验收记录

## 版本与来源

- GitHub 仓库：`linlisWorkTeam/ohMyWorkPanel`
- 发布标签：`v2.1.1`
- 发布标签 commit：`ebe257f1ca3094f432c05b2b66b361995ab3033b`
- ECS 灰度槽：`/opt/ohmyworkpanel/canary`，端口 `8081`
- ECS 生产槽：`8080`，本次未执行 promote，未修改生产服务

## 构建与安装

本地 Windows 构建命令：

```powershell
pnpm install --frozen-lockfile
pnpm run build:web
pnpm tauri build
powershell -NoProfile -File scripts/verify-release.ps1 -ExpectedVersion 2.1.1 -InstallerPath src-tauri\target\release\bundle\nsis\ohMyWorkPanel_2.1.1_x64-setup.exe
```

结果：

- 前端测试：230 passed
- Rust 单元测试：145 passed
- Tauri NSIS 安装包：222,115,177 bytes
- 安装包 SHA-256：`7ccfa9bdb61eef3087e4d131160ba55bd6f805f1aeab99ae5b6d2b8f08053cde`
- Windows WebView2：`offlineInstaller`
- 安装器已在全新临时目录静默安装成功，文件版本为 `2.1.1`

安装包不包含外部 Agent CLI 或登录凭据；Codex、Claude Code、OpenCode、OpenClaw、Cursor 仍需在执行任务的机器上单独安装和登录。

## ECS 灰度验收

- `GET http://127.0.0.1:8081/api/health`：200，返回 `{"ok":true,"service":"ohmyworkpanel"}`
- 8081 首页、当前 JS、当前 CSS：均为 200
- `linlis-work-panel.service`：active
- 8080 生产首页：200
- 旧 `linlis-work-panel-canary.service`：inactive；未停止或重启生产服务

通过灰度 Web UI 完成：

1. `@Codex` 真实 Agent 任务返回 `ECS_REAL_AGENT_OK`。
2. 新增 Mock Agent 后，`@Mock ECS` 返回内置模拟流式回复。
3. 浏览器刷新后，两条消息仍然存在，证明消息已持久化。
4. 认证后的浏览器控制台无错误；登录前一次 `401 /api/adapters` 为预期未认证请求，不作为灰度失败。

截图：`output/playwright/goal-ecs-real-agent.png`（本地验收产物，不随 Release 上传）。

## 边界与未执行项

- 本次只完成 ECS 灰度，没有执行生产 promote；生产晋升仍需人工批准。
- Windows 安装包未进行代码签名，SmartScreen 可能提示风险。
- ECS 首次干净 Rust release 构建在低内存机器上耗时较长，使用单线程构建。

